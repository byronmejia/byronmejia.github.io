export type CSSVariableTypes = 'primaryBackground' | 'primaryColor';

export const PrimaryTheme: Readonly<Record<CSSVariableTypes, string>> = {
    primaryBackground: '#635ee3',
    primaryColor: '#f9faf1',
};
export default PrimaryTheme;
